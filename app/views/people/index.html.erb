<h1>All People</h1>
<br />
<% if @people.any? %>
  <ul>
    <% @people.each do |person| %>
      <li>
        <%= link_to person.name, person_path(person) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h3> No people right right now. Please create some. </h3>
<% end %>

<%= link_to 'New Person', new_person_path %>